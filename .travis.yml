language: objective-c

env:
  global:
    - APM_TEST_PACKAGES="mr-bookmark"

notifications:
  email:
    on_success: never
    on_failure: change

before_script:
  - npm install

script:
  - 'curl -s https://raw.githubusercontent.com/atom/ci/master/build-package.sh | sh'
  - chmod u+x build-package.sh
  - ./build-package.sh

branches:
  only:
    - master

addons:
  apt:
    packages:
    - build-essential
    - git
    - libgnome-keyring-dev
    - fakeroot
